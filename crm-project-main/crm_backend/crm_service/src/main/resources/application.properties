spring.application.name=crm_service

# PostgreSQL Database Config (Non-SSL Server)
spring.datasource.url=jdbc:postgresql://195.250.31.132:5432/crm?ssl=false&sslfactory=org.postgresql.ssl.NonValidatingFactory
spring.datasource.username=crm
spring.datasource.password=75xLPgra!
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Prevent startup crash if DB is slow/unreachable
spring.datasource.hikari.initialization-fail-timeout=0
spring.datasource.hikari.connection-timeout=30000

# Allow app to start even if DB takes time
spring.main.lazy-initialization=true

# Render Dynamic Port Support
server.port=${PORT:8080}

# JWT Secret (Temporary Hardcoded)
jwt.secret=mySuperSecretKey123456789

# Mail Config (optional - can configure later)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Logging
logging.level.org.hibernate.SQL=OFF
logging.level.org.hibernate.type.descriptor.sql=OFF
